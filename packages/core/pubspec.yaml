name: fundus_core
description: Platform-independent Fundus model, database, scanner and import.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  archive: ^4.0.9
  epubx_kuebiko: ^4.2.1
  path: ^1.9.1
  sqlite3: ^3.1.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.30.0
