name: fundus_client
description: Talking to another Fundus over the network.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  crypto: ^3.0.7
  fundus_core: any
  http: ^1.2.2

dev_dependencies:
  fundus_server: any
  lints: ^6.0.0
  shelf: ^1.4.2
  test: ^1.30.0
