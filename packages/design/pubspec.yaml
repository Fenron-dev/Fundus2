name: fundus_design
description: Nocturne design tokens and shared components for the Fundus client.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.1

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  phosphor_flutter: ^2.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
